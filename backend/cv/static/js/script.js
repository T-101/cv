(function ($) {

    "use strict";

    $(document).ready(function () {

        // Animate on Scroll
        AOS.init({
            duration: 600,
            once: true,
        })

    });

})(jQuery);